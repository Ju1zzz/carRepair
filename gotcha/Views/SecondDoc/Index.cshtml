
@{
    ViewBag.Title = "Index";


    Layout = "~/Views/Shared/_Layout.cshtml";


}

<h2>List</h2>

<div class="alert alert-info">
    @using (Html.BeginForm("Result", "SecondDoc", FormMethod.Get))
    {

        <div class="form-group mt-3">
            <div class="col-4">
                <div class="input-group-text">id of guild</div>
                @Html.TextBox("id_g", ViewBag.CurrentFilter as string, new { @class = "form-control", @placeholder = "id of guild" })
            </div>
            <div class="col-4">
                <div class="input-group-text">first letter of part</div>
                @Html.TextBox("letter", ViewBag.CurrentFilter1 as string, new { @class = "form-control", @placeholder = "first letter of part" })
            </div>
        </div>
        <div class="col-md-offset-2 col-4 mt-3">
            <button type="submit" class="btn btn-outline-primary">Find</button>
        </div>
    }
</div>
